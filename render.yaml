services:
  - type: web
    name: decision-simulator-api
    runtime: python
    buildCommand: pip install -r requirements.txt --no-cache-dir
    startCommand: uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
